export const dynamic='force-dynamic';
export async function GET(req,{params}){const {id,folderId}=await params;const base=process.env.CONTENT_API_BASE||'https://nt.studybeepro.site/api/nig';const url=`${base}?content=${encodeURIComponent(id)}&folder=${encodeURIComponent(folderId)}&_t=${Date.now()}`;const r=await fetch(url,{cache:'no-store',headers:{Accept:'application/json',...(process.env.UPSTREAM_API_KEY?{'Authorization':`Bearer ${process.env.UPSTREAM_API_KEY}`}:{})}});const body=await r.text();return new Response(body,{status:r.status,headers:{'content-type':r.headers.get('content-type')||'application/json'}})}
